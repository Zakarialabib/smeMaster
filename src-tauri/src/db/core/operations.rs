//! Core domain operations.
//!
//! Thin re-export layer that surfaces the per-table access helpers from the
//! core domain (accounts, threads, messages, labels, attachments, and so on)
//! through a single module path.

// ── Core domain operations ─────────────────────────────────────────────────
// Re-exports from the existing per-table modules.

//! Workflows domain — higher-level orchestration operations.
//!
//! This module currently has no operations of its own; it exists to group
//! workflow orchestration helpers under `crate::db::workflows`. Per-table
//! data-access lives in `crate::db::tables::workflows`, and the row structs in
//! `crate::db::workflows::schema`.

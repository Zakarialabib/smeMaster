//! Deliverability domain aggregate module: shared schema (`schema`) and
//! operation helpers (`operations`). The per-table CRUD helpers live under
//! `crate::db::tables::deliverability`.

pub mod schema;
pub mod operations;

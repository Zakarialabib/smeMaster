/**
 * Public surface of the license module.
 *
 * Importers should depend on this barrel rather than reaching into
 * individual files. Keeps the public API stable when internals move.
 */

export { LicenseBanner } from './LicenseBanner';
export { LicensePage, type LicensePageProps } from './LicensePage';

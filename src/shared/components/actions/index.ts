/**
 * ActionStatus UI components — visual feedback for async operations.
 *
 * These components consume the ActionStatus store and render appropriate
 * indicators based on the lifecycle state (idle → loading → success | error).
 *
 * Note: ActionButton and ActionStatusBadge were removed in dead-code cleanup.
 * Re-add a barrel export only after the component is recreated.
 */

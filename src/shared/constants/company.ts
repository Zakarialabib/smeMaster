// Active company used across features for multi-tenant data scoping.
// Single source of truth — invoicing previously hard-coded this in its own
// format util; contacts and other features now import from here.
export const ACTIVE_COMPANY_ID = "demo-company-1";
